## ScanDoc

ScanDoc is an Android application developed with Kotlin that scans administrative documents, extracts key information, and organizes it into editable form fields. Users can review, verify, and modify the extracted data before sending it to a designated email address.
